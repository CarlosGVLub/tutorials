<?xml version="1.0"?>
<odoo>
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Propiedades</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
        <field name="context">{'default_active': True}</field>
    </record>
    <record id="estate_property_view_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Buscar Propiedades">
                <field name="name"/>
                <field name="postcode"/>
                <field name="expected_price"/>
                <field name="bedrooms"/>
                <field name="living_area" filter_domain="[('living_area', '>=', self)]"/>
                <field name="facades"/>
                <field name="active"/>
                <separator/>
                <filter string="Disponibles" name="active_properties" domain="['|', ('state', '=', 'new'), ('state', '=', 'offer_received')]"/>
                <filter string="Inactivos" name="inactive_properties" domain="[('active', '=', False)]"/>
                <filter name="Activos" domain="[('active', '=', True)]" help="Mostrar solo propiedades activas"/>
                <!-- <group expand="1" string="Agrupar por">
                    <filter string="Código Postal" name="group_by_postcode" context="{'group_by':'postcode'}"/>
                </group> -->
            </search>
        </field>
    </record>
    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Propiedad">
                <header>
                    <button name="action_set_sold" string="Marcar como Vendida" type="object" class="btn-success" invisible="not (state in ['new', 'offer_accepted'])"/>
                    <button name="action_set_canceled" string="Marcar como Cancelada" type="object" class="btn-danger" invisible="not (state in ['new', 'offer_accepted'])"/>
                    <field name="state" widget="statusbar" statusbar_visible="new,offer_received,offer_accepted,sold,canceled"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Nombre de la propiedad..."/>
                        </h1>
                    </div>
                    <div class="mb-3">
                        <field name="tags_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                    </div>
                    <div class="o_row">
                        <group>
                            <field name="postcode"/>
                            <field name="date_availability"/>
                        </group>
                        <group>
                            <field name="expected_price"/>
                            <field name="selling_price"/>
                        </group>
                        <group>
                            <field name="best_price" readonly="1"/>
                        </group>
                    </div>
                    <notebook>
                        <page string="Descripción">
                            <group>
                                <field name="description" class="oe_full_width"/>
                                <field name="bedrooms" />
                                <field name="living_area" />
                                <field name="facades" />
                                <field name="garage" />
                                <field name="garden" />
                                <field name="garden_area" invisible="not garden"/>
                                <field name="garden_orientation" invisible="not garden"/>
                                <field name="total_area" readonly="1"/>
                                <field name="property_type_id"  options="{'no_create': True}"/>
                            </group>
                        </page>
                        <page string="Ofertas">
                            <field name="offer_ids" readonly="state in ['sold', 'cancelled', 'offer_accepted']">
                                <list editable="bottom" decoration-success="status == 'accepted'" decoration-danger="status == 'refused'">
                                    <field name="price"/>
                                    <field name="partner_id"/>
                                    <field name="status"/>
                                    <button name="action_accept_offer" string="Aceptar Oferta" type="object" icon="fa-check" invisible="not (status != 'accepted')"/>
                                    <button name="action_refuse_offer" string="Rechazar Oferta" type="object" icon="fa-times" invisible="not (status != 'refused')"/>
                                </list>
                            </field>
                        </page>
                        <page string="Información adicional">
                            <group>
                                <field name="salesperson_id" />
                                <field name="buyer_id" />
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>
    <record id="estate_property_view_list" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Propiedades" decoration-muted="state == 'sold'" decoration-success="state in ['offer_received', 'offer_accepted']" decoration-bf="state == 'offer_accepted'">
                <field name="name"/>
                <field name="property_type_id"/>
                <field name="postcode"/>
                <field name="tags_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                <field name="bedrooms"/>
                <field name="living_area"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="date_availability" optional="hide"/>
                <!-- <field name="state"/> -->
            </list>
        </field>
    </record>
</odoo>